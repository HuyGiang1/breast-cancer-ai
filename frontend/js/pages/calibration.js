import{mountShell}from'../components/shell.js';import{researchService}from'../services/research.service.js';import{percent,limitation}from'../components/research.js';mountShell('Calibration & Reliability');const app=document.querySelector('#app');try{const s=await researchService.studies();const dl=s.calibration.dl||[];app.innerHTML=`<section class="research-main"><header class="research-hero"><span class="eyebrow">Probability reliability</span><h1>Calibrated display, unchanged raw classification.</h1><p>Lower Brier and ECE indicate better probability reliability. They do not constitute clinical validation.</p></header><section class="v2-card"><h2>DL validation calibration</h2><div class="table-wrap"><table class="v2-table"><thead><tr><th>Method</th><th>Brier</th><th>ECE (10 bins)</th><th>Log loss</th></tr></thead><tbody>${dl.map(r=>`<tr><th>${r.Method}</th><td>${Number(r.Validation_Brier).toFixed(4)}</td><td>${Number(r.Validation_ECE_10bin).toFixed(4)}</td><td>${Number(r.Validation_LogLoss).toFixed(4)}</td></tr>`).join('')}</tbody></table></div><p>Platt was selected on validation by minimum OOF Brier then log loss.</p></section><section class="v2-card"><h2>Classification contract</h2><p><strong>EfficientNet-B0 decision:</strong> raw malignant probability ≥ 0.515. <strong>Platt probability:</strong> display/reliability only; it does not replace the decision probability space.</p></section><section class="v2-card"><h2>ML calibration</h2><p>Logistic Regression: raw retained. Random Forest: Platt. XGBoost: raw retained. Calibration decisions were development-based.</p></section>${limitation(s.evidence.limitations)}</section>`}catch(e){app.textContent=e.message}
