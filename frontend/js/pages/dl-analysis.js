import{mountShell}from'../components/shell.js';import{predictionService}from'../services/prediction.service.js';import{resultCard}from'../components/analysis.js';mountShell('Mammography DL Analysis');const app=document.querySelector('#app');app.innerHTML=`<section class="research-main"><header class="research-hero"><span class="eyebrow">EfficientNet-B0 · CBIS-DDSM</span><h1>Mammography research analysis</h1><p>Full processed image representation. Selection does not establish clinical adequacy.</p></header><section class="analysis-columns"><form id="analysis" class="v2-card"><label class="v2-upload" id="drop">Choose or drop JPEG/PNG<input id="file" type="file" accept="image/jpeg,image/png" required></label><img id="preview" class="analysis-preview" alt="Selected image preview" hidden><p id="meta">No image selected.</p><button class="v2-button">Analyze image</button><button class="v2-button secondary" id="remove" type="button">Remove</button></form><div id="result" class="v2-card">Select an image for research model analysis.</div></section></section>`;const form=document.querySelector('#analysis'),input=document.querySelector('#file'),preview=document.querySelector('#preview'),meta=document.querySelector('#meta'),drop=document.querySelector('#drop');let file;const select=f=>{if(!f?.type.match(/^image\/(jpeg|png)$/)){meta.textContent='Use a JPEG or PNG image.';return}file=f;preview.src=URL.createObjectURL(f);preview.hidden=false;meta.textContent=`${f.name} · ${Math.ceil(f.size/1024)} KB · ${f.type}`};input.onchange=()=>select(input.files[0]);drop.ondragover=e=>e.preventDefault();drop.ondrop=e=>{e.preventDefault();select(e.dataTransfer.files[0])};document.querySelector('#remove').onclick=()=>{file=null;input.value='';preview.hidden=true;preview.removeAttribute('src');meta.textContent='No image selected.'};form.onsubmit=async e=>{e.preventDefault();if(!file)return;const button=form.querySelector('button');button.disabled=true;document.querySelector('#result').textContent='Uploading image and running EfficientNet-B0…';try{const r=await predictionService.dl(file);document.querySelector('#result').innerHTML=resultCard(r,{dataset:'CBIS-DDSM · full processed image',threshold:.515,calibrated:true})}catch(err){document.querySelector('#result').textContent=err.message}finally{button.disabled=false}};
