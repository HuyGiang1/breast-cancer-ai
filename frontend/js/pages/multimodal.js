import{mountShell}from'../components/shell.js';import{predictionService}from'../services/prediction.service.js';import{ML_GROUPS,ML_FEATURES}from'../config/ml-features.js';import{featureSection,collectFeatures,resultCard}from'../components/analysis.js';mountShell('Experimental Fusion');const app=document.querySelector('#app');app.innerHTML=`<section class="research-main experimental"><header class="research-hero"><span class="v2-badge">EXPERIMENTAL RESEARCH DEMO</span><h1>Unpaired output-combination heuristic</h1><p>WDBC features and CBIS-DDSM images are separate, unpaired datasets. The 40% ML + 60% DL rule is a software demonstration, not a validated multimodal model, scientific fusion result, or clinical score.</p></header><form id="fusion">${ML_GROUPS.map(g=>featureSection(...g)).join('')}<section class="v2-card"><label class="v2-field">Research image<input name="image" type="file" accept="image/jpeg,image/png" required></label><button class="v2-button">Run experimental combination</button></section></form><div id="result"></div></section>`;const form=document.querySelector('#fusion');form.onsubmit=async e=>{e.preventDefault();const image=form.elements.image.files[0];if(!image)return;const button=form.querySelector('button');button.disabled=true;const out=document.querySelector('#result');out.textContent='Running two frozen models and combining outputs…';try{const r=await predictionService.multimodal({clinicalData:collectFeatures(form,ML_FEATURES),imageFile:image});out.innerHTML=`<div class="analysis-columns">${resultCard(r.ml_result,{dataset:'WDBC',threshold:.36})}${resultCard(r.dl_result,{dataset:'CBIS-DDSM',threshold:.515,calibrated:true})}</div><section class="v2-card"><span class="v2-badge">Experimental heuristic</span><h2>Experimental combined score: ${(Number(r.combined_confidence)*100).toFixed(1)}%</h2><p>ML 40% + DL 60%. This is not a unified diagnosis.</p></section>`}catch(err){out.textContent=err.message}finally{button.disabled=false}};
