import{mountShell}from'../components/shell.js';import{researchService}from'../services/research.service.js';import{researchTable,limitation}from'../components/research.js';mountShell('Model Comparison');const app=document.querySelector('#app');try{const s=await researchService.studies();app.innerHTML=`<section class="research-main"><header class="research-hero"><span class="eyebrow">Comparison, not a shared leaderboard</span><h1>Candidate trade-offs within each study.</h1><p>Metrics are frozen held-out evidence. ML and DL are never ranked against each other.</p></header>${researchTable('WDBC Classical ML · primary candidate: Logistic Regression',s.ml)}<p class="v2-card">Logistic Regression remains primary because selection was development-first, not because of a retrospective held-out ranking.</p>${researchTable('CBIS-DDSM Deep Learning · retained candidate: EfficientNet-B0',s.dl,'Model')}<p class="v2-card">ResNet50 has higher sensitivity with lower specificity; EfficientNet-B0 provides stronger discrimination and balance.</p>${limitation(s.evidence.limitations)}</section>`}catch(e){app.textContent=e.message}
