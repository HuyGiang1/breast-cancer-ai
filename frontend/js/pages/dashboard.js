import{mountShell}from'../components/shell.js';import{modelService}from'../services/model.service.js';import{researchService}from'../services/research.service.js';import{percent}from'../components/research.js';mountShell('Overview');const app=document.querySelector('#app');app.innerHTML='<section class="v2-card" aria-busy="true">Loading research workspace…</section>';try{const[status,s]=await Promise.all([modelService.finalStatus(),researchService.studies()]);const ml=s.ml.find(x=>x.model==='Logistic Regression'),dl=s.dl.find(x=>x.Model==='EfficientNet-B0');app.innerHTML=`<section class="research-main"><header class="research-hero"><span class="eyebrow">Research platform overview</span><h1>Welcome${status?.user?.full_name?`, ${status.user.full_name}`:''}</h1><p>WDBC structured ML and CBIS-DDSM mammography DL are separate studies.</p><p><a class="v2-button" href="research.html">Research Center</a> <a class="v2-button secondary" href="../login.html">New analysis requires migrated workflow</a></p></header><section class="v2-grid"><article class="v2-card"><span class="eyebrow">Final ML · WDBC</span><h2>${ml.model}</h2><p>ROC-AUC ${percent(ml.roc_auc)} · Sensitivity ${percent(ml.sensitivity)} · Specificity ${percent(ml.specificity)}</p><span class="v2-badge">${ml.threshold} raw · ${status.ml?.status||'Unavailable'}</span></article><article class="v2-card"><span class="eyebrow">Final DL · CBIS-DDSM</span><h2>${dl.Model}</h2><p>ROC-AUC ${percent(dl['ROC-AUC'])} · Sensitivity ${percent(dl.Sensitivity)} · Specificity ${percent(dl.Specificity)}</p><span class="v2-badge">0.515 raw · Platt display only · ${status.dl?.status||'Unavailable'}</span></article><article class="v2-card"><span class="eyebrow">System health</span><h2>${status.ml?.status==='research_demo'&&status.dl?.status==='research_demo'?'Research demo ready':'Status unavailable'}</h2><p>Model availability is read from the final status endpoint. No patient/prediction counts are fabricated.</p></article></section><section class="v2-grid"><article class="v2-card"><h2>Dataset snapshot</h2><p>${s.wdbc.samples} WDBC samples / ${s.wdbc.features} features</p><p>${s.cbis.processedImages} CBIS-DDSM processed images / ${s.cbis.groups} inferred groups</p></article><article class="v2-card"><h2>Research capability</h2><p>SHAP + Grad-CAM · calibration + bootstrap analysis · frozen runtime.</p></article><article class="safety-note"><strong>Research / Educational Prototype</strong><p>Not for clinical diagnosis.</p></article></section></section>`}catch(e){app.innerHTML=`<section class="v2-card"><h1>Workspace unavailable</h1><p>${e.message}</p></section>`}
