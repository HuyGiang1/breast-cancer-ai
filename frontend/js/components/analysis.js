import{featureLabel}from'../config/ml-features.js';import{probabilityBar}from'./probability-bar.js';
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
export const featureSection=(title,features)=>`<section class="feature-section"><h2>${title}</h2><div class="feature-fields">${features.map(key=>`<label class="v2-field" for="${key}">${featureLabel(key)}<input class="v2-input" id="${key}" name="${key}" type="number" min="0" step="any" required></label>`).join('')}</div></section>`;
export const resultCard=(r,{dataset,threshold,calibrated=false}={})=>`<section class="v2-card v2-result" aria-live="polite"><span class="eyebrow">Model prediction</span><h2>${esc(r.diagnosis)}</h2><p><strong>Raw malignant probability:</strong> ${(Number(r.raw_probability??r.probability)*100).toFixed(1)}%</p>${probabilityBar(r.raw_probability??r.probability,threshold,'Raw malignant probability')}<p>Decision threshold: ${threshold} raw</p>${calibrated?`<hr><p><strong>Reliability-adjusted display probability:</strong> ${(Number(r.calibrated_probability??r.probability)*100).toFixed(1)}%</p><p>Platt calibration · display/reliability only</p>`:''}<dl><dt>Model</dt><dd>${esc(r.model_name)}</dd><dt>Dataset</dt><dd>${esc(dataset)}</dd><dt>Clinical use</dt><dd>false</dd></dl><p class="safety-note">Research / Educational Prototype. Not for clinical diagnosis.</p></section>`;
export const collectFeatures=(form,features)=>Object.fromEntries(features.map(key=>[key,Number(form.elements[key].value)]));
