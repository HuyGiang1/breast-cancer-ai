const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const patientForm=(p={})=>`<form id="patientForm" class="v2-card"><h2>${p.id?'Edit':'Add'} patient</h2><input type="hidden" name="id" value="${esc(p.id||'')}"><label class="v2-field">Full name<input class="v2-input" name="full_name" value="${esc(p.full_name)}" required></label><label class="v2-field">Date of birth<input class="v2-input" name="date_of_birth" type="date" value="${esc(p.date_of_birth)}"></label><label class="v2-field">Gender<input class="v2-input" name="gender" value="${esc(p.gender)}"></label><label class="v2-field">Notes<textarea class="v2-input" name="notes">${esc(p.notes)}</textarea></label><p id="formError" aria-live="polite"></p><button class="v2-button">Save patient</button></form>`;
export const predictionRow=(r,reportUrl)=>`<article class="workspace-row"><div><span class="v2-badge">${esc(r.prediction_type)}</span><strong>${esc(r.diagnosis||'No label')}</strong><small>${esc(r.created_at)}</small></div><p>${esc(r.model_name||'Model unavailable')} · ${r.raw_probability==null?'Probability unavailable':`${(Number(r.raw_probability)*100).toFixed(1)}% raw`}</p>${reportUrl?`<a href="${reportUrl}" target="_blank" rel="noopener">Open HTML report</a>`:''}</article>`;
export{esc};
